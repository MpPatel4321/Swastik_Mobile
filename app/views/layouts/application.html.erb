<!DOCTYPE html>
<html>
  <head>
    <title>Social</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"
    ></script>
  </head>

  <body>
    <% if current_user %>
      <nav class="navbar navbar-expand-lg navbar-light bg-light
              navbar-inverse navbar-fixed-top mb-4">
        <%= link_to root_path, class: 'navbar-brand' do %>
          <h3 class="text-primary"> <%= current_user.name %> </h3>
        <% end %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <%#= link_to users_path, class: 'nav-link' do %>
              <!-- <image src="/assets/find_friend.png" class="avatar float-left"> -->
            <%# end %>
          </li>
          <li class="nav-item <%= root_path %>">
            <%= link_to 'Add Category', new_category_path, class: "nav-link text-primary" %>
          </li>
          <% categories = Item.where(item_type: 'category') %>
          <% unless categories.blank? %>
            <li class="nav-item <%= root_path %>">
              <%= link_to 'Add Company', new_company_path, class: "nav-link text-primary" %>
            </li>
            <li class="nav-item <%= root_path %>">
              <%= link_to 'Add Model', new_model_path, class: "nav-link text-primary" %>
            </li>
          <% end %>
          <li class="nav-item <%= root_path %>">
            <%= link_to 'Create Bill', new_bill_path, class: "nav-link text-primary" %>
          </li>
        </ul>
        <div class="pull-right">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%#= link_to friend_requests_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%#= link_to messages_path(nav_bar: false), class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%#= link_to user_path(current_user), class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Log-Out', destroy_user_session_path, method: :delete, class: "nav-link text-danger" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  <% end %>
  <div class='container' role='main'>
    <%= yield %>
  </div>